---
const navItems = [
  { label: "Mode", href: "/fashion" },
  { label: "Beauté", href: "/beauty" },
  { label: "Voyage", href: "/travel" },
  { label: "Maison & Jardin", href: "/home-garden" },
  { label: "Technologie", href: "/technology" },
];
const currentPath = Astro.url.pathname;
---

<header class="site-header">
  <div class="header-inner">
    <a href="/" class="site-logo">Orlisse<span>Living</span></a>
    <nav class="main-nav">
      {navItems.map(item => (
        <a
          href={item.href}
          class={currentPath.startsWith(item.href) ? "active" : ""}
        >
          {item.label}
        </a>
      ))}
    </nav>
  </div>
</header>
