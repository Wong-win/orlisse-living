{
  "name": "orlisse-living",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "postbuild": "git add -A && git commit -m \"Build: $(date '+%Y-%m-%d %H:%M')\" || true && git push || true && bash deploy.sh || true",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^5.0.0"
  }
}
